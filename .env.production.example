# Production Environment Variables
# Copy this to .env and fill in the values before deployment
# NEVER commit the .env file with real secrets!

# ======================
# REQUIRED SECRETS
# ======================
DATABASE_PASSWORD=your-strong-database-password-here
MINIO_ACCESS_KEY=your-minio-access-key-here
MINIO_SECRET_KEY=your-minio-secret-key-here
JWT_SECRET=your-super-secret-jwt-key-at-least-32-chars
JWT_REFRESH_SECRET=your-refresh-secret-key-at-least-32-chars

# ======================
# OPTIONAL OVERRIDES
# ======================
DATABASE_NAME=filestorage
DATABASE_USER=postgres

# Your domain (used for frontend API URL)
API_URL=https://your-domain.com/api/v1

# ======================
# GENERATE SECRETS
# ======================
# Use these commands to generate secure random secrets:
#
# Linux/Mac:
#   openssl rand -base64 32
#
# PowerShell:
#   [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 }) -as [byte[]])
#
# Or use: https://generate-secret.vercel.app/32
